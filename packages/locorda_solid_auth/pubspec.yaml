name: locorda_solid_auth
description: Locorda Solid Auth - Solid authentication implementation for locorda using solid-auth
version: 0.5.1
homepage: https://locorda.dev/sync-engine/
repository: https://github.com/locorda/sync-engine
issue_tracker: https://github.com/locorda/sync-engine/issues
resolution: workspace

topics:
  - solid
  - authentication
  - oidc

environment:
  sdk: ^3.6.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  logging: ^1.3.0
  locorda_core: ^0.5.1
  locorda_solid_core: ^0.5.1
  locorda_ui: ^0.5.1
  url_launcher: ^6.3.1

  # Solid authentication
  solid_oidc_auth: ^0.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ">=5.1.1 <7.0.0"

flutter:
  generate: true

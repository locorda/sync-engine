name: locorda_mapping_bootstrap_generator
description: Build-time generator for embedding CRDT mapping documents (merge strategy definitions).
version: 0.5.0
homepage: https://locorda.dev/sync-engine/
repository: https://github.com/locorda/sync-engine
issue_tracker: https://github.com/locorda/sync-engine/issues
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  build: ^4.0.0
  glob: ^2.1.2

dev_dependencies:
  test: ^1.26.3
  build_test: ^3.0.0

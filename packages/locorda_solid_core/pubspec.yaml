name: locorda_solid_core
description: Solid Pod backend implementation for Locorda — internal HTTP layer and types used by locorda_solid. One of multiple BYOB backends alongside Google Drive.
version: 0.5.1
resolution: workspace
homepage: https://locorda.dev/sync-engine/
repository: https://github.com/locorda/sync-engine
issue_tracker: https://github.com/locorda/sync-engine/issues
documentation: https://locorda.dev/

topics:
  - rdf
  - linked-data
  - semantic-web
  - crdt
  - solid

environment:
  sdk: ^3.6.0

dependencies:
  logging: ^1.2.0
  rxdart: ^0.28.0

  # Core locorda dependencies
  locorda_core: ^0.5.1
  
  locorda_rdf_core: ^0.12.2

  # HTTP client for Solid Pod communication
  http: ^1.1.0

dev_dependencies:
  test: ^1.26.3
  lints: ">=5.1.1 <7.0.0"
name: locorda_solid
description: Locorda Solid - Sync offline-first apps using your user's remote storage
version: 0.1.0-dev
publish_to: none
publisher: locorda.dev
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  locorda_core: any
  locorda_flutter_core: any
  locorda_solid_core: any
  locorda_solid_auth: any
  locorda_worker: any
  locorda_solid_auth_worker: any

  # RDF
  locorda_rdf_core: ^0.12.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0
  test: ^1.26.3

flutter:
  uses-material-design: true

topics:
  - rdf
  - crdt
  - solid
  - offline-first
  - sync
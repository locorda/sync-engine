name: locorda_builder
description: Build-time transformations for Locorda - web worker compilation, asset copying, and future CRDT code generation
version: 0.1.0-dev
publisher: locorda.dev
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  build: ^4.0.0
  build_config: ^1.1.0
  analyzer: ">=8.1.0 <11.0.0"
  path: ^1.9.0

dev_dependencies:
  test: ^1.26.3
  build_test: ^3.0.0

topics:
  - build
  - code-generation
  - web-workers

name: locorda_builder
description: "Build-time Locorda transformations: assembles worker manifests from transitive dependencies, compiles Dart Isolates/Web Workers to JS, and automates asset handling."
version: 0.5.2
homepage: https://locorda.dev/sync-engine/
repository: https://github.com/locorda/sync-engine
issue_tracker: https://github.com/locorda/sync-engine/issues
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  build: ^4.0.0
  build_config: ^1.1.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.26.3
  build_test: ^3.0.0

topics:
  - build
  - code-generation
  - web-workers
  - build-runner

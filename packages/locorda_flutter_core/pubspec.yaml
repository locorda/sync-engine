name: locorda_flutter_core
description: Locorda Flutter Core - Sync offline-first apps using your user's remote storage
version: 0.5.0
homepage: https://locorda.dev/sync-engine/
repository: https://github.com/locorda/sync-engine
issue_tracker: https://github.com/locorda/sync-engine/issues
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  locorda_core: ^0.5.0
  locorda_worker: ^0.5.0
  locorda_ui: ^0.5.0
  
  # RDF mapping
  locorda_rdf_core: ^0.12.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.26.3

flutter:
  uses-material-design: true

topics:
  - rdf
  - crdt
  - solid
  - offline-first
  - sync